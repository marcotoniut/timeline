/**
 * List of details of an `Event` in the Timeline
 * @note Type definitions help guide you when interfacing with functions and other structures
 */
export interface EventData {
  id: number;
  time: string;
  title: string;
  description: string;
}
